@{
    ViewData["Title"] = "Dashboard";
}

<div class="container-fluid p-4">
    <div class="card shadow-sm border-0 rounded-3 p-5 text-center bg-white">
        <h1 class="display-4 fw-bold text-primary mb-3">Welcome to Dashboard</h1>
        <p class="lead text-muted">You are now logged into the School Management System.</p>
    </div>
</div>